import { StyleSheet } from 'react-native'

const heading = {
    padding: 30,
    fontSize: 50,
};

const container = {
    flexDirection: 'column',
    alignItems: 'center',
};

const box = {
    flexDirection: 'row',
    flexWrap: 'wrap',
    justifyContent: 'space-around',
    alignContent: 'center',
};

const styles = {
    heading: heading,
    container: container,
    box: box
};

export {styles}